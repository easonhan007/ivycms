json.extract! setting, :id, :name, :url, :logo, :ico, :copyright, :email, :skype, :whatsapp, :phone, :product_per_page, :post_per_page, :recommend_per_page, :share_code, :email_user_name, :email_password, :email_port, :email_smtp, :email_receiver, :email_sender, :email_active, :ga_code, :created_at, :updated_at
json.url setting_url(setting, format: :json)
